{
        "version": 2,
        "buildCommand": "cd client && npm run build && cd ..",
        "outputDirectory": "client/build",
        "env": {
                  "NODE_ENV": "production"
        }
}
